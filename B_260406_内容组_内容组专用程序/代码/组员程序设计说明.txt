这样吧，我把我要的应用程序逻辑全部跟你说一遍，你根据这个来别瞎改了，首先我们要弄清楚内容组有什么任务，第一收集每个广播剧每一集的URL，非常驻角色，有无其他配音演员，片名，第二，人工审核有无其他配音演员里填了有的剧集，第三抽检的10%文件去人工复核一遍，第四为技术组兜底，如果技术组解决不了，爬不出来的就给内容组，技术组需要提供一个“信息表”给内容组，内容组组长把这个表设置到电脑里，这是第四步的任务分配源。
然后说说任务先后逻辑关系，
广播剧剧集里有几个任务，任务底下有具体的剧集名，然后每个任务都有收集→审核→抽检的环节，必须环环相扣，前一个做完了才可以解锁该任务下一个阶段，但是每个任务的完成阶段是单独统计的。这是组长要管的事情，我只是告诉你。第四步的兜底是独立于任何一个环节的

然后“广播剧剧集.xlsx”数据结构是这样的，这是一个总表，组长要分配的任务分别写在第一行的不同列，第二行是一个任务要占两列，第一列写"完整剧集名”，第二列写"ddl”。第二行第一列往下都是剧集名，是组员具体要处理的小任务，第二行第二列全是ddl，然后审核用的“审核表”，抽检用的“抽检表”都是这个数据结构。绑定任务的时候，就是一人绑定一列。当然一个人也可以认领多个任务。绑定任务和ddl的时候是从第三行往下，逐个绑定。然后第一行用第一第二列写第一个任务，第一行第三第四列写第二个任务，是这样的。
然后组员认领的时候填写的是第一行的任务名!!


然后我现在来说说程序运行逻辑，组长和组员是两套程序，因为不联网，所以一套程序也没用。也不用判断有没有重复领取。
先说组员的程序，这是最简单的。你不需要给我写组长端，两个一起写你太笨了，别浪费你算力。

首先，在组员第一次打开这个程序的时候，界面只有一个"欢迎使用该程序，该程序主要用于认领任务，确认ddl，以及最后的文件命名规范化。现在请告诉我您的cn，方便署名和持续使用。"，先询问组员ID，这一步提示组员“请输入您的圈名，文件命名需要用到”，然后程序需要存下这个数据，不要第二次打开的时候这个数据就没了，就是这个程序和这个名字是绑死的，不可以再改，后面绑定任务啊ddl啊文件规范命名啊都要用这个名字。所以请在他输入一遍后，弹出信息"确认后不可更改，请您再输入一遍。"，如果两次输入不一致，请提示她"检测到输入有出入，请确认您的cn"，然后再让她输入一次，输入完弹出信息"确认后不可更改，请您再输入一遍。"然后让她在输入一遍，输入成功以后弹出"欢迎( 她的名字)加入逐声计划!！"然后弹出下一个交互界面，让她选1信息收集，2.重点剧集审核，3.人工抽检复核
第二次打开不必再问是谁，直接输出"欢迎(她的名字)回来，请输入回车进入程序!"，第三次第四次第n次打开都是这样。
如果ddl只剩四天了，就要改成，
"欢迎(她的名字)回来，请输入回车进入程序!
请注意，您的(看她是哪个阶段的任务)任务距离ddl还剩()天，请合理安排时间，非常感谢您"
然后每个环节里都应该设置个进度查询及ddl查询的功能，要让组员可以在这个功能里知道自己提交了多少文件，还差多少文件，所以程序要自动统计她认领的任务列有几个剧集，知道ddl是什么时候，距离ddl还有多少天。如果她这个环节没有任务，就提醒"您暂时没有领取该流程的任务哦"
如果该流程没有存表，或者表格为空，就在每个环节"查看可认领任务"那里提醒他"当前没有任务可领取"

以下是信息收集阶段

第一，组员需要认领自己的工作，也就是组长把“广播剧剧集.xlsx”发在群里，组员保存，然后程序读取这个xlsx，程序自己生成一个任务列表，然后程序告诉组员你现在有哪些任务可以领取，并询问她要认领几个任务，组员在交互界面先填写认领几个任务，如果是一个任务，就填写要认领的任务，但是填写的必须跟程序生成的列表里一模一样，然后领取完告诉组长。领取完这个任务（也就是“广播剧剧集”绑定一列）就跟这个组员绑定了，程序要记录这个数据知道吧，不能关闭以后就没了。
如果是多个任务，交互界面应该先弹出一个交互信息“好的，现在你确认你要认领（）个任务，请确保您的时间充裕，如若权衡后觉得时间较紧张，请输入q退出，然后与组长自行商量，确认认领请输入OK”，操作人按下q后就返回任务认领界面询问她要认领几个任务。如果她确认认领，那么程序先后弹出“请告诉我您认领的第（）个任务是：”然后程序以此把任务那几列进行绑定。

第二，领取的同时，程序要自动读取“广播剧剧集.xlsx”里的ddl列，然后这个ddl也和这个组员绑定，这个数据也保存下来。

第三，这个程序可以供组员自行查看自己任务的进度，看剩余数量，距离ddl还有多少天

第四，组员将“任务名.txt”文件上传到“信息待检查文件夹”以后，然后程序开始对这些文件自动规范命名，这是组员端的功能。（规范命名为“任务名_采集人_日期_版本号.csv），组员的文件里任务名和“广播剧剧集.csv”去自动匹配换成完整任务名，因为前面任务和认领人已经绑定，采集人就直接替换成当前操作人的ID，然后日期自动获取她操作那一天的日期，版本号询问操作人，你这是第几版，然后统一改成这个版本号，版本号应该是“V加数字”，然后每个命名规范的文件要直接覆盖原来的文件！！然后提醒组员记得将他们移出“信息待检查文件”，避免重复检查。
收集阶段，组员会把每个任务包含的剧集的信息放在一个TXT里，不能用剧集名去匹配，应该用任务名去匹配!!!这点很重要!!!然后命名规范应该是“任务名_操作人_日期_版本号.csv”，其他三个环节是用剧集名!!!
以上是收集的阶段。


以下是审核阶段。

第一，组员需要认领自己的工作，也就是组长把“审核表.xlsx”发在群里，组员保存，然后程序读取这个xlsx，程序自己生成一个任务列表，然后程序告诉组员你现在有哪些任务可以领取。并询问她要认领几个任务，组员在交互界面先填写认领几个任务，如果是一个任务，就填写要认领的任务，但是填写的必须跟程序生成的列表里一模一样，然后领取完告诉组长。领取完这个任务（也就是“审核表.xlsx”绑定一列）就跟这个组员绑定了，程序要记录这个数据知道吧，不能关闭以后就没了。
如果是多个任务，交互界面应该先弹出一个交互信息“好的，现在你确认你要认领（）个任务，请确保您的时间充裕，如若权衡后觉得时间较紧张，请输入q退出，然后与组长自行商量，确认认领请输入OK”，操作人按下q后就返回任务认领界面询问她要认领几个任务。如果她确认认领，那么程序先后弹出“请告诉我您认领的第（）个任务是：”然后程序以此把任务那几列进行绑定。

第二，领取的同时，程序要自动读取“审核表.xlsx”里的ddl列，然后这个ddl和这个组员绑定，这个数据也保存下来。

第三，这个程序可以供组员自行查看自己任务的进度，看剩余数量，距离ddl还有多少天

第四，组员将“剧集名.txt”文件上传到“审核待检查文件夹”以后，然后程序开始对这些文件自动规范命名，（规范命名为“审核完整剧名_采集人_日期_版本号.txt），组员的文件里的剧集名和“审核表.xlsx”去自动匹配换成完整剧名，因为前面任务和认领人已经绑定，所以就匹配已经绑定的任务剧集名，采集人就直接替换成当前操作人的ID，然后日期自动获取她操作那一天的日期，版本号询问操作人，你这是第几版，然后统一改成这个版本号，版本号应该是“V加数字”，然后每个命名规范的文件要直接覆盖原来的文件！！然后提醒组员记得将他们移出“审核待检查文件”，避免重复检查。
以上是审核的阶段。

以下是抽检环节

第一，组员需要认领自己的工作，也就是组长把“抽检表.xlsx”发在群里，组员保存，然后程序读取这个xlsx，程序自己生成一个任务列表，然后程序告诉组员你现在有哪些任务可以领取。并询问她要认领几个任务，组员在交互界面先填写认领几个任务，如果是一个任务，就填写要认领的任务，但是填写的必须跟程序生成的列表里一模一样，然后领取完告诉组长。领取完这个任务（也就是“抽检表.xlsx”绑定一列）就跟这个组员绑定了，程序要记录这个数据知道吧，不能关闭以后就没了。
如果是多个任务，交互界面应该先弹出一个交互信息“好的，现在你确认你要认领（）个任务，请确保您的时间充裕，如若权衡后觉得时间较紧张，请输入q退出，然后与组长自行商量，确认认领请输入OK”，操作人按下q后就返回任务认领界面询问她要认领几个任务。如果她确认认领，那么程序先后弹出“请告诉我您认领的第（）个任务是：”然后程序以此把任务那几列进行绑定。

第二，领取的同时，程序要自动读取“抽检表.xlsx”里的ddl列，然后这个ddl和这个组员绑定，这个数据也保存下来。

第三，这个程序可以供组员自行查看自己任务的进度，看剩余数量，距离ddl还有多少天

第四，组员将“剧集名.txt”文件上传到“审核待检查文件夹”以后，然后程序开始对这些文件自动规范命名，（规范命名为“抽检完整剧名_采集人_日期_版本号.txt），组员的文件里的剧集名和“抽检表.xlsx”去自动匹配换成完整剧名，因为前面任务和认领人已经绑定，所以就匹配已经绑定的任务剧集名，采集人就直接替换成当前操作人的ID，然后日期自动获取她操作那一天的日期，版本号询问操作人，你这是第几版，然后统一改成这个版本号，版本号应该是“V加数字”，然后每个命名规范的文件要直接覆盖原来的文件！！然后提醒组员记得将他们移出“抽检待检查文件”，避免重复检查。
以上是抽检的阶段。

以下是兜底的阶段

第一，组员需要认领自己的工作，也就是组长把“需人工转接.xlsx”发在群里，组员保存，然后程序读取这个xlsx，程序自己生成一个任务列表，然后程序告诉组员你现在有哪些任务可以领取。并询问她要认领几个任务，组员在交互界面先填写认领几个任务，如果是一个任务，就填写要认领的任务，但是填写的必须跟程序生成的列表里一模一样，然后领取完告诉组长。领取完这个任务（也就是“抽检表.xlsx”绑定一列）就跟这个组员绑定了，程序要记录这个数据知道吧，不能关闭以后就没了。
如果是多个任务，交互界面应该先弹出一个交互信息“好的，现在你确认你要认领（）个任务，请确保您的时间充裕，如若权衡后觉得时间较紧张，请输入q退出，然后与组长自行商量，确认认领请输入OK”，操作人按下q后就返回任务认领界面询问她要认领几个任务。如果她确认认领，那么程序先后弹出“请告诉我您认领的第（）个任务是：”然后程序以此把任务那几列进行绑定。

第二，领取的同时，程序要自动读取“需人工转接.xlsx”里的ddl列，然后这个ddl和这个组员绑定，这个数据也保存下来。

第三，这个程序可以供组员自行查看自己任务的进度，看剩余数量，距离ddl还有多少天

第四，组员将“剧集名.txt”文件上传到“需人工转接待检查文件夹”以后，然后程序开始对这些文件自动规范命名，（规范命名为“抽检完整剧名_采集人_日期_版本号.txt），组员的文件里的剧集名和“抽检表.xlsx”去自动匹配换成完整剧名，因为前面任务和认领人已经绑定，所以就匹配已经绑定的任务剧集名，采集人就直接替换成当前操作人的ID，然后日期自动获取她操作那一天的日期，版本号询问操作人，你这是第几版，然后统一改成这个版本号，版本号应该是“V加数字”，然后每个命名规范的文件要直接覆盖原来的文件！！然后提醒组员记得将他们移出“需人工转接待检查文件”，避免重复检查。



最后，程序要询问组员到底在哪个阶段，对应开启任务界面，程序的话，抽检就找“抽检表.xlsx”，审核就找“审核表.xlsx”，信息收集就找“广播剧剧集.xlsx”。

代码里不要有emoji，严格按照我的要求来，不要跳任何一步。能不能就是她在信息收集那里操作完只按0返回菜单界面啊，不要直接退出程序，在菜单界面设置按0退出就行